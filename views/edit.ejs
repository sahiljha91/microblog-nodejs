<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body>

    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <h3 class="text-3xl mb-5">Edit Post</h3>
     <form action="/update/<%=post._id%>" method="POST">
           <textarea placeholder=""class="w-1/3 resize-none bg-transparent border-2 border-zinc-800 rounded-md p-2 mb-5" name="content" id=""><%= post.content%></textarea>
           <input class="px-3 w-40 py-2 bg-yellow-500 text-black block" type="submit" name="" value="Update Post">
         </form>

    </div>
</body>
</html>`