<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body>

    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">

        <div class="w-full flex justify-end">
        <a class ="bg-red-500 rounded-md px-3 py-2 text-sm mb-5"href="/logout">Logout</a>
        </div>

     <h3 class="text-3xl mb-5"><span class="font-light">Hello,<%=user.name%></span></h3>
       <h5 class="mb-5">Create a new post.</h5>
         <form action="/post" method="POST">
           <textarea placeholder="What's on your mind"class="w-1/3 resize-none bg-transparent border-2 border-zinc-800 rounded-md p-2 mb-5" name="content" id=""></textarea>
           <input class="px-3 w-40 py-2 bg-blue-500 block" type="submit" name="" value="Create new post">
         </form>
         <div class="posts">
            <h3 class="mt-20">YOUR POST</h3>
            <div class="container">
                <% user.posts.reverse().forEach(function(post){  %>  

                <div class="mt-4 w-1/3 border-2 p-5 rounded-md bg-zinc-700 border-zinc-900">
                    <h4 class="text-blue-600 mb-2"><%= user.username %></h4>
                    <p class="tracking-tight"><%=post.content %></p>
                    <div class="text-sm mt-1 inline-block"><%=post.like.length %> likes </div>
                      <div class="btns flex gap-10">
                        <a class="text-blue-500 hover:text-blue-400 mt-3"href="/like/<%=post._id%>">
                            <%= post.like.indexOf(user._id)===-1?"Like":"Unlike"   %> 
                        </a>
                        <a class="text-blue-500 hover:text-blue-400 mt-3"href="/edit/<%=post._id%>">Edit</a>
                        <a class="text-blue-500 hover:text-blue-400 mt-3"href="/delete/<%=post._id%>">Delete</a>
                      </div>
                </div>

                <% }) %>
                </div>
            </div>
            </div>
         </div>
    </div>
</body>
</html>